{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "consumer-run": "node login-service/",
    "consumer-component-test": "jest --testPathPattern=login-service/tests",
    "consumer-contract-test": "jest --testPathPattern=login-service/tests/contract-tests",
    "provider-run": "node user-service/",
    "provider-component-test": "jest --testPathPattern=user-service/tests/component-tests",
    "provider-contract-test": "jest --testTimeout=10000 --testPathPattern=user-service/tests/contract-tests"
  },
  "author": "up1",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^10.1.3",
    "jest": "^29.0.3",
    "nock": "^13.2.9",
    "supertest": "^6.2.4"
  }
}
